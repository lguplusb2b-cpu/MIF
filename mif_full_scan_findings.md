# MIF 앱 전체 스캔 진단 노트 (2026-08-19)

## 1. 코드 구조 실측

- App.tsx 6,752줄 단일 파일에 화면 22개, 시트 13개, 공통 컴포넌트 20여 개가 모두 포함됨.
- 업무 규칙 모듈: workflows / product-workflows / category-workflows / order-presentation / navigation-workflows / address-workflows / role-workflows / auth-workflows / notification-workflows.
- 서버: server/index.ts 452줄, Express 라우트 47개, PostgreSQL raw SQL.
- 클라이언트 API 계약(src/api.ts)에는 메서드 8개만 존재 → 서버 47개 라우트 중 대부분이 앱에서 호출되지 않음.

## 2. 기술 품질 스캔

- `pnpm check`(tsc --noEmit) 오류 없음.
- `pnpm test`(Vitest) 12개 파일·42개 테스트 전부 통과.
- 린트 스크립트는 package.json에 없음(정적 분석 게이트 부재).
- 로컬 API 서버는 현재 미실행. `EXPO_PUBLIC_MIF_API_URL` 미설정 시 `isMifApiConfigured()`가 false가 되어 앱이 전면 로컬 AsyncStorage 모드로 동작.

## 3. 인증 오류 테스트 관찰

- 거래처 계정 `mif`에 잘못된 비밀번호(`wrongpass`)로 로그인 시도 → 로그인되지 않고 로그인 화면에 머무름(정상 차단).
- 다만 실패 안내는 `Alert.alert`로 처리되어 웹 미리보기에서는 화면에 아무 메시지도 표시되지 않음. DOM에도 안내 문구가 없음.
- App.tsx 전체에서 `Alert.alert` 35회 사용 → 웹 미리보기 기준으로는 로그인·저장·삭제 등 다수 피드백이 보이지 않을 수 있음(모바일 기기에서는 정상 표시).

## 4. 데이터·재고 구조 관찰

- 재고는 `stockStatus`(in_stock / out_of_stock) 2단계 상태만 존재. 수량 기반 재고나 주문 시 차감 로직 없음.
- 로컬 계정 비밀번호는 `src/domain.ts`에 평문 문자열로 보관(`password: "mif"`, `password: "admin1234"`).
- 주문 상태 전이는 `nextOrderStatus` 단방향 + CANCELED 예외로 제한됨.

## 5. 관리자 프로세스 오류 테스트 관찰

- 관리자 로그인 후 관리자 업무 화면에 주문·상품·카테고리·결제은행·가입 심사·계정 권한·비밀번호 재설정·입점 문의·공지·Q&A 등 10개 관리 메뉴가 모두 노출됨.
- 상품 등록 시트에서 모든 필수 항목을 비운 채 저장하면 등록은 차단되지만(상품 0/0개 유지), 어떤 항목이 부족한지 화면에 표시되지 않음. 검증 실패 안내도 `Alert.alert` 경로여서 웹에서는 무음 실패로 보임.
- 관리자 상품 화면에서도 검색·전체상품·정렬 컨트롤이 정상 노출되어, 거래처와 동일한 상품 탐색 UI를 공유함.

## 6. 상품 등록 → 장바구니 → 주문 프로세스 테스트

- 정상 입력(상품명·직접 입력 카테고리·규격·단위·최소수량 2·단가 18,000)으로 저장하자 상품 1/1개로 즉시 반영되고, 재고 있음/실온 배지와 카테고리 라벨이 함께 표시됨.
- 상품 담기 시 최소 수량 2가 자동 적용되고, 계속 담기·장바구니로 이동 선택이 있는 담기 완료 모달이 정상 동작함.
- 장바구니에서 배송지·배송 방법·희망일이 모두 비어 있는 상태로 주문하기를 누르면 주문이 생성되지 않음(정상 차단). 다만 무엇이 누락됐는지에 대한 화면 안내가 없어 사용자는 원인을 알 수 없음.
- 장바구니 결제 계좌 영역이 "결제 계좌는 관리자가 등록합니다."로만 표시됨. 기본 결제 계좌가 없으면 거래처가 입금 정보를 확인할 수 없어 초기 운영 시 결제 안내 공백이 발생함.

## 7. 배송지·주소 검색 관찰

- 배송지 등록 시트는 배송지명·수령인·연락처·주소·상세 주소·기본 배송지 설정을 갖추고 있어 항목 구성은 충분함.
- 다만 "주소 선택" 모달은 우편번호·도로명·지번을 **직접 타이핑**하는 수동 입력 폼이며, 실제 주소 검색(도로명주소 API/다음 우편번호) 연동은 없음. 거래처가 정확한 우편번호를 외워야 하는 구조로 오입력 위험이 큼.

## 8. 주문 생성 결과와 발견된 권한 문제

- 배송지 저장 후 택배 선택 → 기본 배송지 사용 → 주문하기로 `MIF-20260819-0001` 주문이 정상 생성됨. 주문 상세에 배송 방법·배송지·희망일·금액이 모두 표시됨.
- **문제 1(권한)**: 관리자 계정(`admin`)으로 로그인한 상태에서도 장바구니 담기와 주문 생성이 가능했고, 생성된 주문의 거래처가 "MIF 거래처"로 기록됨. 관리자가 발주 주체가 되어 데이터가 오염될 수 있음.
- **문제 2(권한)**: 관리자 더보기 화면에 "거래처 가입 신청"과 "비밀번호 재설정" 메뉴가 그대로 노출됨. 이미 로그인한 관리자에게 불필요한 메뉴임.
- **문제 3(결제)**: 주문 상세 결제 정보에 "등록된 결제 계좌가 없습니다."가 표시됨. 결제 계좌 미등록 상태로도 주문이 생성되어 입금 안내가 누락됨.
- 주문 상세 액션은 재주문·주문 취소·결제확인 처리·주문 삭제가 모두 노출되어 관리자 상태 전이 기능은 정상 배치됨.

## 9. 확인된 버그: 주문 상세 상태 미갱신

주문 상세 시트에서 "결제확인 처리"를 눌러도 상세 화면의 상태 배지가 계속 `접수`로 표시된다. 원인은 `changeOrderStatus`가 `data.orders` 배열만 갱신하고, 시트에 전달되는 `selectedOrder` 상태(App.tsx 275행)는 갱신하지 않기 때문이다. `OrderSheet`는 `order={selectedOrder}`로 렌더링되므로 상태 변경 결과가 열려 있는 상세에 반영되지 않는다. 목록으로 돌아가면 반영되지만, 상세에서 연속 처리(접수→결제확인→상품준비)를 진행할 수 없다.

주문 목록으로 이동하면 상태는 정상적으로 `결제확인`으로 반영되고, 상태 칩 카운트(전체 1 / 결제확인 1)와 주문 카드의 배송 방법·배송지 표시도 정확했다. 상단 알림 배지도 1건으로 증가해 알림 생성 흐름은 동작한다. 따라서 데이터 계층은 정상이며 상세 시트 리렌더링만 문제다.

## 10. 발주톡 기준 미구현·미연결 기능

| 영역 | 발주톡 기준 | MIF 현재 상태 |
| --- | --- | --- |
| 송장·배송조회 | 택배사·송장번호 등록, 배송조회 진입, 용달 기사 연락 | 앱 화면에 송장/배송조회 UI 없음. 서버에는 `PATCH /api/orders/:id/status`(송장 필드)와 `GET /api/orders/:id/tracking`이 있으나 tracking은 501 응답이고 앱은 호출하지 않음 |
| 주문 엑셀·CSV 내보내기 | 관리자 주문 내보내기 | 서버 `GET /api/orders/export`(CSV) 존재, 앱에 버튼 없음 |
| 상품 이미지 업로드 | 서버 S3 업로드 | 서버 `POST /api/uploads/product-image` 존재, 앱은 로컬 이미지 선택만 사용 |
| 찜 공유 | 찜 목록 공유 링크 | 앱은 OS `Share.share`만 사용. 서버 `POST/GET /api/favorite-shares` 토큰 API 미연결 |
| 세션 유지 | 자동 로그인 | `src/storage.ts`에 세션 저장 키 없음 → 앱을 다시 열면 매번 재로그인 필요 |
| 비밀번호 변경 | 로그인 후 비밀번호 변경 | 재설정 "요청"만 있고, 승인 후 실제 비밀번호 변경 화면 없음 |
| 상품 목록 페이지네이션 | 페이지 단위 로딩 | 전체 목록을 한 번에 렌더링(상품 수 증가 시 성능 리스크) |
| 상품 상세 이미지 확대 | 이미지 확대 보기 | 미구현 |

가장 큰 구조적 격차는 **서버 라우트 47개 대비 앱이 호출하는 API가 8개**라는 점이다. 현재 앱은 `EXPO_PUBLIC_MIF_API_URL`이 없으면 전면 로컬 AsyncStorage 모드로 동작하므로, 기기별로 데이터가 분리되어 관리자와 거래처가 같은 주문을 공유할 수 없다. 실제 다중 사용자 운영을 위해서는 상품·주문·장바구니·공지·Q&A·배송지·계좌를 서버 API로 전환해야 한다.

## 11. 상품 관리 화면 추가 관찰

- 관리자 계정으로 상품 카드를 누르면 **상품 상세 화면이 아니라 "상품 수정" 시트**가 바로 열린다. 관리자도 거래처가 보는 상세 화면(상세 이미지·설명)을 확인할 경로가 없다.
- 상품 수정 시트에 **상품 삭제/판매중지 버튼이 없다**. 잘못 등록한 상품을 목록에서 제거할 수단이 없어 데이터가 계속 누적된다.
- 상품 수정 시트는 상품명·카테고리·규격·단위·최소수량·단가·설명·대표/상세 이미지·재고 상태·보관 상태·마케팅 배지를 모두 갖추고 있어 입력 항목 자체는 발주톡 기준을 충족한다.

## 12. 알림 화면 관찰

- 알림 목록에 "새 발주가 접수되었습니다"(주문번호 포함)가 정상 표시되고, 모두 읽음 처리 버튼도 존재한다.
- 다만 알림 항목을 눌러도 해당 주문 상세로 이동하지 않는다(딥링크 없음). 관리자가 주문 탭으로 다시 찾아가야 한다.
- 결제확인으로 상태를 변경했지만 관리자 본인이 처리한 상태 변경 알림은 목록에 추가되지 않았다. 역할별 수신 대상 분리는 동작하는 것으로 보이나, 거래처 계정으로 로그인해 상태 변경 알림 수신을 별도 확인해야 한다.
- 미확인 알림 배지 숫자는 화면 상단에 유지되어 즉시 인지 가능하다.
- 모두 읽음 처리는 정상 작동해 배지가 사라졌으나, **개별/전체 알림 삭제 기능이 없어** 알림이 무한 누적된다. 오래된 알림 정리 정책(보관 기간 또는 삭제)이 필요하다.
- 관리자 홈은 "금일 발주 N건"과 접수·배송중 건수를 표시해 발주톡의 오늘 발주 대시보드 개념을 재현하고 있다.
