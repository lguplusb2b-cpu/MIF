# 홈 공지 활성화 검증

## 원인

클라이언트의 공지 활성화 판단이 `Date#toISOString()`의 UTC 날짜를 사용했다. 공지 시작일·종료일과 관리자 시트의 기본 날짜는 한국 운영일 기준 `YYYY-MM-DD`이므로, 한국 자정 전후에는 당일 활성 공지가 기간 외처럼 처리될 수 있었다. 또한 홈 공지 행은 상세를 열 수 있는 상호작용이 연결되지 않았다.

## 수정

- 공지 활성화 기준을 `Asia/Seoul` 현지 날짜로 통일했다.
- 활성 공지는 노출 기간을 적용한 뒤 최신 수정일·생성일 순으로 정렬한다.
- 홈 공지 행과 거래처 공지 목록을 누르면 읽기 전용 공지 상세 모달이 열린다.
- 관리자 공지 저장 후 `MifData.notices` 갱신을 통해 홈 공지 영역이 즉시 다시 계산된다.

## 미리보기 확인

1. 빈 데이터 상태에서 테스트 데이터를 불러오자 당일 공지 `MIF 미리보기 테스트 안내`가 홈에 표시됐다.
2. 홈 공지 행을 누르자 제목·운영 공지 배지·노출일·본문을 포함한 상세 모달이 열렸다.
3. 관리자 공지 관리에서 `당일 운영 공지`를 시작일 `2026-08-19`, 즉시 노출 상태로 저장했다.
4. 홈으로 돌아오자 새 공지가 기존 공지보다 위에 즉시 표시됐다.

단위 테스트는 비노출 공지·기간 종료 공지를 제외하고 활성 공지를 최신순으로 정렬하는 규칙, 그리고 한국 운영일 당일의 노출 판정을 포함한다. 검증 후 **빈 상태로 초기화**를 실행해 테스트 전용 공지·상품·배송지·계정·알림 데이터를 제거했고, MIF는 실제 운영 데이터가 없는 초기 빈 상태로 복귀했다.
