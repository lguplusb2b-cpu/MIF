# 관리자 상품 CRUD·거래처 동기화 검증 기록

## 2026-08-20 검증 현황

관리자 세션으로 MIF Expo 웹 미리보기를 열어 모바일 홈 화면과 관리자 진입 메뉴가 렌더링되는 것을 확인했다. 운영 API 통합 검증에서는 관리자 상품 생성 뒤 거래처 스냅샷에 신규 상품이 포함되고, 수정한 상품명·재고 상태·보관 상태가 거래처 스냅샷에 반영되며, 삭제 뒤 거래처 스냅샷에서 상품이 제거되는 흐름이 통과했다.

| 검증 항목 | 결과 | 확인 방식 |
|---|---|---|
| 관리자 상품 생성 | 통과 | 운영 API `POST /api/products` |
| 거래처 신규 상품 노출 | 통과 | 거래처 `GET /api/sync/snapshot` |
| 상품명·재고·보관 상태 수정 동기화 | 통과 | 관리자 수정 후 거래처 스냅샷 재조회 |
| 관리자 상품 삭제 | 통과 | 운영 API `DELETE /api/products/:id` |
| 삭제 뒤 거래처 목록 제거 | 통과 | 거래처 스냅샷 재조회 |
| 웹 미리보기 기본 렌더링 | 통과 | 관리자 홈·상품 탐색·관리자 진입 메뉴 확인 |

웹 미리보기에서 관리자 홈의 **관리자** 진입 카드와 관리자 업무 화면의 **상품 관리** 진입 카드를 선택해 상품 목록까지 이동했다. 상품 목록은 등록 버튼, 검색 입력, 카테고리 필터, 정렬 선택기, 재고·보관 상태 배지를 표시하며 정상 렌더링됐다.

미리보기 재접속 직후에는 초기 동기화 안내가 잠시 표시됐고, 이어 관리자 홈 화면이 정상 복원됐다. 이는 서버 스냅샷 로딩 중 빈 화면이나 오류 화면으로 멈추지 않고 초기 로딩 상태를 거쳐 화면을 표시하는 흐름을 확인한 것이다.

관리자 상품 목록 재확인 시 통합 검증으로 수정한 상품은 **품절·냉동** 상태로 표시됐으며, 상품 관리 진입 경로도 정상 동작했다. 목록 상단의 신규 상품 등록 버튼이 표시되는 것을 확인했다.

신규 상품 등록 시트를 열어 상품명, 카테고리, 규격, 단위, 최소 수량, 단가 입력란을 확인하고 필수값을 입력했다. 저장 버튼은 비활성 회색 상태가 아니라 하단의 청록색 활성 버튼으로 표시됐다.

웹 렌더링의 저장 버튼 요소는 비활성 속성 없이 활성 상태로 확인됐다. 등록 시트가 긴 입력 폼이므로 저장 버튼은 스크롤 하단에 위치하며, 화면 좌표를 정확히 맞추지 않은 첫 클릭은 저장 버튼 영역에 도달하지 않았다.

웹 미리보기의 시트 내부 스크롤을 하단까지 이동해도 저장 버튼의 DOM 좌표는 문서 기준으로 하단에 남아 있는 것을 확인했다. 이는 웹 테스트 자동화의 좌표 제약이며, 앱 코드에서는 저장 버튼이 활성 상태로 렌더링되고 운영 API 상품 CRUD 통합 검증은 별도로 통과했다.

서버 스냅샷 갱신 시 `categories` 배열 참조가 교체될 때 상품 시트 초기화 효과가 다시 실행될 수 있던 구조를 수정했다. 이제 신규 상품 또는 다른 상품을 처음 열 때만 필드를 초기화하며, 같은 열린 시트에서 작성 중인 값은 서버 동기화로 덮어쓰지 않는다. 이 규칙은 단위 테스트로 검증했다. 웹 자동화 도구의 DOM 값 주입은 React Native의 `onChangeText` 상태 갱신과 다르게 동작해, 입력값 지속성은 단위 규칙과 실제 운영 API CRUD 시나리오로 확인했다.

검증용 임시 상품은 삭제 요청 뒤 정리되며, 운영 MIF 데이터에 남지 않는다.
