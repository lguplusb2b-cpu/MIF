# 관리자 공지 노출 기간 달력 모달 검증 기록

## 구현 확인

- 공지 등록·수정 시트의 노출 시작일과 종료일을 텍스트 입력 대신 날짜 선택 버튼으로 교체했다.
- 두 버튼은 월 이동 화살표와 날짜 그리드를 포함한 달력 모달을 연다.
- 종료일 모달에서는 시작일 이전 날짜를 선택할 수 없고, 시작일 모달에서는 이미 설정된 종료일 이후 날짜를 선택할 수 없다.
- 종료일은 **종료일 없음**으로 해제할 수 있으며, 저장 시 시작일·종료일 범위를 다시 검증한다.

## 미리보기 진행 상태

최신 Expo 웹 미리보기가 정상 렌더링되는 것을 확인했다. 기존 상품 등록 시트를 닫은 뒤 하단 홈 탭으로 복귀했고, 관리자 업무 진입 카드가 표시되는 것을 확인했다.

관리자 업무 화면에서 **공지 관리**로 이동해 공지사항 목록과 우측 상단 **추가** 버튼이 정상 표시되는 것을 확인했다. 다음 단계로 신규 공지 시트를 열어 시작일·종료일 선택 모달을 검증한다.

캐시 영향을 배제하기 위해 새 주소로 미리보기를 다시 열었고, 최신 앱 번들이 관리자 홈 화면까지 정상 로드되는 것을 확인했다.

새로 고친 화면에서도 관리자 업무의 공지 관리 목록까지 정상적으로 다시 진입했다.

신규 공지 등록 시트에서 기존 텍스트 입력 대신 **시작일**과 **종료일** 날짜 선택 버튼이 표시되는 것을 확인했다. 시작일 버튼을 누르면 `2026년 8월` 월 표기, 이전·다음 달 화살표, 요일 그리드, 날짜 선택, 오늘, 선택 완료 버튼을 포함한 달력 모달이 정상적으로 열렸다.

다음 달 화살표를 선택하자 달력 제목과 날짜 그리드가 `2026년 9월`로 갱신됐고, 9월 15일 선택 동작도 정상적으로 처리됐다.

선택 완료 후 시작일 버튼 값이 `2026-09-15`로 갱신되고 모달이 닫혔다. 종료일 모달을 열면 시작일 전 날짜와 이전 달 이동 버튼이 비활성화되고, 9월 15일 이후의 날짜만 선택 가능하게 표시됐다. 종료일 없음과 선택 완료 버튼도 표시됐다.

종료일로 `2026-09-16`을 선택하고 완료하자 모달이 닫히며 등록 시트의 종료일 버튼 값이 해당 날짜로 즉시 갱신됐다. 시작일과 종료일이 모두 달력 선택값으로 표시되는 것을 확인했다.
