# 관리자 카테고리 CRUD 및 상품 연동 검증

## 오류 원인

관리자 카테고리 관리 화면의 추가·수정 동작은 `sheet` 상태를 `category`로 변경했지만, 이를 실제로 렌더링하고 저장하는 `CategorySheet` 구성 요소가 앱 루트에 없었다. 따라서 상단 추가 버튼과 수정 아이콘을 눌러도 입력 화면이 열리지 않았다.

## 수정 내용

- `CategorySheet`를 추가하고 `sheet === "category"` 상태에 연결했다.
- 카테고리명·아이콘·노출 순서·상품 화면 노출 여부를 입력·수정할 수 있게 했다.
- 카테고리명 중복과 빈 값은 저장하지 않도록 검증했다.
- 수정 시 연결 상품의 `categoryName`을 즉시 갱신하고, 삭제 시 연결 상품을 `미분류`로 전환한다.
- 활성 카테고리만 상품 목록 필터와 상품 등록 선택지에 노출하며, 노출 순서대로 정렬한다.

## 검증 시작 상태

최신 Expo 미리보기에서 테스트 전용 카테고리 3개와 상품 3개가 정상 렌더링됐다. 이후 관리자 역할에서 카테고리 추가·수정·삭제 후 거래처 상품 화면의 필터와 상품 분류가 바로 갱신되는지 확인한다.

거래처 역할에서는 관리자 업무가 표시되지 않았고, 미리보기 역할을 관리자로 전환한 뒤에만 관리자 업무 메뉴가 노출됐다. 카테고리 CRUD는 관리자 역할에서만 검증한다.

관리자 업무에서 카테고리 관리에 정상 진입했고, 신선식품·가공식품·소모품의 노출 순서와 사용 상태가 정렬된 목록으로 표시됐다. 추가 버튼과 각 행의 수정·삭제 아이콘이 모두 노출됐다.

추가 버튼을 누르자 기존 오류와 달리 **카테고리 등록** 시트가 열렸다. 카테고리명·아이콘·노출 순서·상품 화면 노출 스위치와 저장 버튼이 표시됐고, 신규 검증 카테고리 `냉동식품`·`🧊`·순서 `4` 입력이 정상 반영됐다.

카테고리 추가를 저장하자 시트가 닫히고 관리자 목록에 `🧊 냉동식품 · 노출 순서 4 · 사용`이 즉시 추가됐다. 이어서 해당 행의 수정 아이콘을 눌러 기존 값이 채워진 **카테고리 수정** 시트가 정상적으로 열리는 것을 확인했다.

추가한 카테고리명을 `냉장·냉동식품`으로 변경해 저장하자, 시트가 닫힌 뒤 관리자 목록의 해당 항목 이름이 즉시 갱신됐다.

수정한 검증 카테고리의 삭제 아이콘을 누르자 해당 행이 즉시 목록에서 제거됐다. 이어서 상품이 연결된 `신선식품`을 `친환경 신선식품`으로 수정·저장했고, 관리자 카테고리 목록의 이름 갱신을 확인했다. 다음 단계에서 거래처 상품 카드와 필터에 연결 상품의 새 분류명이 반영됐는지 확인한다.

카테고리 관리 화면을 닫아 홈으로 돌아오자 추천 상품 카드의 분류명이 `친환경 신선식품`으로 즉시 변경돼 표시됐다. 상품 탭에서도 활성 카테고리 필터가 `🥬 친환경 신선식품`으로 갱신됐고, 테스트 친환경 채소 상품 카드의 분류명도 같은 값으로 동기화됐다.

삭제 동기화 검증을 위해 관리자 역할을 유지한 채 상품 탭에서 관리자 업무로 다시 이동했다. 다음 단계에서 상품이 연결된 카테고리를 삭제한 뒤 해당 상품이 `미분류`로 전환되는지 확인한다.

카테고리 관리에서 상품이 연결된 `친환경 신선식품`을 삭제하자 해당 카테고리 행이 즉시 목록에서 제거됐다. 상품 화면으로 이동해 테스트 친환경 채소의 분류가 `미분류`로 전환되고 필터에서 삭제한 카테고리가 사라졌는지 이어서 확인한다.

홈 화면의 테스트 친환경 채소 카드가 삭제 직후 `미분류`로 변경됐고, 상품 탭에서도 해당 상품이 `미분류`로 표시됐다. 활성 카테고리 필터에서는 삭제한 `친환경 신선식품`이 사라지고 `가공식품`·`소모품`만 남았다. 이로써 추가·수정·삭제 결과가 상품 분류와 필터에 즉시 반영되는 것을 확인했다.

검증 종료 후 **빈 상태로 초기화**를 실행해 테스트 전용 카테고리·상품·주소·계좌·공지·계정 데이터를 제거했다. 현재 미리보기는 실제 운영 데이터가 이관되지 않은 MIF의 빈 상태로 복귀했다.
