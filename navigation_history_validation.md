# 상단 종료·뒤로가기 및 Android 시스템 뒤로가기 검증 기록

## 수정 내용

- 모든 페이지 이동을 화면 이력에 기록하도록 `recordPageTransition`과 `getPreviousPage` 규칙을 추가했다.
- 루트 탭(홈·상품·주문·장바구니·더보기)에서는 홈을 제외한 화면에 상단 **뒤로가기**와 **종료(X)** 버튼을 함께 표시한다.
- 세부 화면은 기존 `BackHeader`의 뒤로가기·종료 버튼을 유지하되, 종료(X)도 홈이 아닌 직전 화면으로 복귀하도록 통일했다.
- Android `BackHandler`가 시스템 뒤로가기를 가로채 시트·계속담기 안내를 먼저 닫고, 이후에는 직전 화면으로 이동한다. 이력이 없는 홈 화면에서는 앱 이탈을 요청하지 않는다.
- 모든 뒤로가기·종료 버튼에 접근성 레이블을 지정했다.

## 미리보기 검증

1. 거래처 홈에서 상품 탭으로 이동하자 상단에 `이전 화면으로 이동`과 `이전 화면으로 닫기`가 함께 표시됐다.
2. 상품 탭의 종료(X)를 누르자 홈으로 복귀했다. 이 경우 홈이 실제 직전 화면이었다.
3. 홈 → 더보기 → 앱 정보로 이동한 뒤 앱 정보의 종료(X)를 누르자 홈으로 강제 이동하지 않고 **더보기**로 복귀했다.
4. 관리자 역할에서 더보기 → 관리자 업무로 이동한 뒤 관리자 업무의 종료(X)를 누르자 역시 **더보기**로 복귀했다.
5. 세부 화면에는 `BackHeader`의 뒤로가기와 종료(X)가 함께 표시되는 것을 확인했고, 루트 탭은 앱 바의 뒤로가기·종료 제어를 사용한다.

웹 미리보기에서는 Android의 실제 하드웨어 뒤로가기 신호를 생성할 수 없으므로, 해당 분기는 `BackHandler` 구현과 탐색 이력 단위 테스트로 확인했다. 실제 Android APK에서는 시스템 뒤로가기가 앱을 이탈하지 않고 위 우선순위에 따라 시트·직전 화면을 닫는다.
