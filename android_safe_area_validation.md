# MIF Android 안전 영역 검증

## 적용 내용

- 루트에 `SafeAreaProvider`를 추가하고 앱 본문을 상단·좌우 안전 영역을 적용하는 `SafeAreaView`로 감쌌다.
- Android 상태 표시줄은 불투명 배경과 어두운 아이콘을 사용하도록 설정했다.
- 하단 탭은 기기 하단 안전 영역 인셋만큼 높이·여백을 확보하고, 세부 페이지 본문도 하단 인셋을 피해 배치한다.
- Expo Android 설정에서 `edgeToEdgeEnabled`를 꺼서 시스템 영역 뒤에 앱 UI가 배치되지 않도록 했다.

## 미리보기

최신 Expo 웹 번들이 정상 로드됐고, 세로형 MIF 앱 프레임에서 상단 앱 바와 하단 다섯 개 탭이 각각 독립된 영역에 렌더링되는 것을 확인했다. Android 기기에서는 네이티브 안전 영역 인셋이 추가로 적용된다.

안전 영역 보정 후에도 하단 상품 탭과 주문 탭을 차례로 선택했을 때 정상적으로 화면이 전환됐다. 상단 앱 바의 계정·알림·닫기 제어와 하단 탭이 화면 콘텐츠와 겹치지 않고 유지되는 것을 확인했다.
