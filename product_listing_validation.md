# 상품 검색·카테고리·정렬 검증 기록

## 구현 기준

- 상품 검색 시 상품명·규격·카테고리명으로 결과를 찾고, 입력과 동시에 선택 카테고리를 `전체상품`으로 전환한다.
- 카테고리 바의 맨 앞에는 항상 `전체상품`을 두며, 활성 카테고리는 MIF teal 색상으로 강조한다.
- 정렬 선택기는 `인기순`, `가격 높은순`, `가격 낮은순`, `최신순`을 제공한다.
- 상품 수는 `상품 필터 결과/전체 상품개` 형식으로 표시한다.

## 2026-08-19 미리보기 1차 확인

변경 후 기존 웹 미리보기의 HMR 반영이 지연되어 이전 UI(`전체`, `최신순`, 이전 검색 안내문)가 표시됐다. Expo 웹 미리보기 서버를 재시작한 뒤 최신 번들이 홈 화면까지 정상 로드되는 것을 확인했다. 거래처 상품 화면에는 검색 입력창, 카테고리 맨 앞의 teal 강조 `전체상품` 칩, `상품 0/0개` 결과 수, 기본 `인기순` 선택기가 표시됐다. 정렬 선택기를 열어 `인기순`, `가격 높은순`, `가격 낮은순`, `최신순` 네 옵션과 현재 선택 표시가 모두 노출되는 것도 확인했다. 빈 운영 데이터 상태에서도 전체상품과 정렬 UI가 정상 표시되는 것을 확인했으며, 더보기의 테스트 데이터 불러오기 기능으로 상품·카테고리·검색 결과를 추가 검증할 준비를 마쳤다. 테스트 데이터 불러오기 클릭 중 일시적 미리보기 연결 오류가 있었으나, Expo 서버 실행 상태를 확인한 뒤 재접속하여 홈 화면이 정상 복구되는 것을 확인했다. 이후 격리된 테스트 데이터를 불러와 신선식품·가공식품·소모품 카테고리의 상품 3개가 준비된 것을 확인했다. 상품 탭에서 `전체상품` 뒤에 세 카테고리가 표시되고, 신선식품 선택 시 `상품 1/3개`와 해당 상품 1개만 표시되는 것을 확인했다. 이어 신선식품 선택 상태에서 `즉석밥`을 검색하자 전체상품이 자동 선택되어 가공식품 테스트 즉석밥 1개가 검색됐고, 검색어 지우기 버튼도 표시됐다. 검색어를 지운 뒤 전체상품의 3개 목록으로 복귀했고, 정렬 선택기에 네 가지 옵션이 다시 모두 노출되는 것을 확인했다. 가격 높은순 선택 후 선택기 문구가 `가격 높은순`으로 바뀌었고, 15,800원 즉석밥·12,000원 친환경 채소·3,900원 위생장갑 순으로 목록이 정렬됐다. 가격 낮은순을 선택하자 3,900원 위생장갑·12,000원 친환경 채소·15,800원 즉석밥 순으로 반전됐으며, 최신순 항목도 같은 정렬 선택기에서 선택 가능한 상태를 확인했다. 최신순 선택 후 선택기 문구가 `최신순`으로 바뀌고 최근 등록된 친환경 채소가 목록 첫 번째에 표시되는 것을 확인했다. 검증을 마친 뒤 더보기 화면에서 격리된 테스트 데이터를 빈 상태로 초기화했고, 홈 화면이 상품·배송지·알림 등이 없는 MIF 기본 상태로 복구된 것을 확인했다.

## 2026-08-19 모바일 반응형 검증

전체상품 칩이 수평 스크롤 끝에서 가려질 수 있는 현상을 확인해, 이를 가로 스크롤 바깥의 고정 칩으로 분리했다. 보완 후 타입 검사와 40개 단위 테스트를 통과했으며, Expo 웹 미리보기를 재시작해 최신 번들과 격리된 테스트 상품이 정상 로드되는 것을 확인했다.

보완 후 320px 폭에서 전체상품 칩은 프레임 안의 517px~561px 위치에 고정되고, 일반 카테고리만 185px 뷰포트·275px 콘텐츠·90px 스크롤 범위로 분리되어 동작했다. 검색 입력 유효 폭은 216px을 유지했으며 프레임과 문서 수준의 가로 오버플로는 다시 발생하지 않았다.

375px 폭에서도 전체상품 칩은 프레임 안에 고정되고, 일반 카테고리만 240px 뷰포트·275px 콘텐츠·35px 스크롤 범위에서 표시됐다. 검색 입력 유효 폭은 271px이었고, 프레임과 문서 수준의 가로 오버플로는 발생하지 않았다.

375px 폭에서 신선식품 칩을 선택하자 `상품 1/3개`와 친환경 채소 1개만 표시됐다. 이어 `즉석밥`을 검색하자 전체상품으로 자동 전환되어 가공식품 테스트 즉석밥 1개가 표시됐고, 검색어 지우기 버튼도 입력 영역 안에 유지됐다.

반응형 검증을 마친 뒤 격리된 테스트 데이터를 빈 상태로 초기화했다. 홈 화면이 주문·상품·배송지·알림이 없는 MIF 기본 상태로 돌아온 것을 확인했다.

일반 카테고리를 오른쪽 끝까지 이동한 상태에서도 전체상품 칩은 계속 보였다. 같은 320px 폭에서 `위생장갑`을 입력하자 검색어 지우기 버튼이 검색 상자 안에 표시되고, 전체상품 기준으로 `상품 1/3개` 및 위생장갑 결과 1개가 정상 표시됐다.

세로형 MIF 모바일 프레임의 빈 데이터 홈 화면과 더보기 화면을 확인했다. 이어 격리된 테스트 데이터를 불러온 뒤 320px와 375px 너비에서 상품 검색 입력, 카테고리 가로 스크롤, 전체상품 고정, 정렬 선택기와 결과 목록의 줄바꿈·오버플로·터치 반응을 점검한다. 테스트 데이터를 불러온 세로형 프레임의 상품 화면에는 검색 입력, 전체상품, 세 개 카테고리, 결과 수, 정렬 선택기와 찜 버튼이 모두 세로로 겹치지 않고 표시됐다. 기본 430px 프레임에서 검색 상자의 폭은 380px, 검색 입력의 유효 폭은 326px이며, 문서 수준의 가로 오버플로는 감지되지 않았다. 다음 단계에서는 같은 앱 프레임을 375px와 320px로 조정해 검색·카테고리·정렬 컨트롤의 최소 폭 동작을 확인한다. 375px 폭에서는 검색 상자 325px·입력 유효 폭 271px을 확보했고, 프레임과 문서 수준의 가로 오버플로가 발생하지 않았다. 카테고리 바는 361px 뷰포트에서 368px 콘텐츠를 수평 스크롤로 처리하므로 칩을 줄바꿈하지 않고 탐색할 수 있다. 320px 폭에서는 검색 상자 270px·입력 유효 폭 216px을 확보했고, 카테고리 바는 306px 뷰포트와 368px 콘텐츠 사이의 62px 차이를 수평 스크롤로 처리한다. 정렬 선택기와 찜 버튼의 오른쪽 경계도 프레임 안에 남았고, 프레임·문서 수준의 가로 오버플로는 없었다. 320px 프레임의 시각 확인에서도 검색 영역·카테고리 바·정렬 행·상품 카드가 겹치거나 잘리지 않았다. 카테고리 바는 화면 좌표 기준 487px, 321px에 놓이며 62px 범위의 실제 가로 스크롤이 가능하다. 자동 스크롤 도구는 React Native Web의 해당 컨테이너를 이동시키지 못했으나, 동일 컨테이너의 `scrollLeft`를 끝값 62px으로 적용해 끝 위치 도달을 확인했다. 다만 끝 위치에서는 전체상품 칩의 좌우 범위가 카테고리 바 밖으로 이동해 보이지 않는 것을 확인했다. 발주톡 기준에 맞게 전체상품을 수평 스크롤 바깥의 고정 칩으로 분리해 보완한다.
