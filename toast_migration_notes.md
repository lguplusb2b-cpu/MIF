# MIF 인앱 토스트 도입 작업 노트

## 구현 완료

- `src/toast-workflows.ts`: 토스트 생성·중복 병합·최대 3개 큐·톤별 노출 시간·접근성 라벨 규칙
- `src/toast-workflows.test.ts`: 위 규칙 단위 테스트 8건 (통과)
- `App.tsx`
  - 기존 단일 `feedback` 배너 상태를 `toasts: MifToast[]` 큐로 교체
  - 공용 함수: `showToast`, `notify`, `notifySuccess`, `notifyError`, `notifyWarning`, `showFeedback`(호환용)
  - 250ms 간격 만료 정리 `useEffect`
  - `ToastStack` 컴포넌트: 상단 오버레이(`insets.top + 58`), 톤별 아이콘·좌측 강조선, 탭하면 즉시 닫힘
  - 스타일: `toastStack`, `toastCard`, `toastTitle`, `toastMessage`, `toastClose`
  - 전환 완료: 권한 변경, 찜 공유, 주문 일괄 처리

## 톤 기준

| 톤 | 노출 | 용도 |
| --- | --- | --- |
| success | 2.6초 | 저장·주문·처리 완료 |
| info | 3.0초 | 일반 안내 |
| warning | 3.6초 | 선택 누락 등 사용자 조치 필요 |
| error | 4.2초 | 검증 실패·서버 오류 |

## 전환 완료 범위

`App.tsx` 내 `Alert.alert` 호출은 **0건**으로 전부 제거되었다. 전환 결과는 성격에 따라 두 갈래로 정리했다.

| 성격 | 처리 방식 | 대상 |
| --- | --- | --- |
| 결과·검증 안내 | 인앱 토스트 | 로그인 성공·실패, 권한 변경, 찜 공유, 주문 일괄 처리, 택배 배송희망일 제한, 기본 배송지 없음, 배송 희망일 미선택, 가입 신청 검증, 비밀번호 재설정 검증, 배송지·우편번호 검증, 주소 조회 검증, 결제 계좌 검증, 카테고리명 검증, 공지 제목·내용 검증, Q&A 검증, 입점 문의 검증 |
| 파괴적 작업 확인 | 인앱 확인 모달 (`ConfirmModal`) | 장바구니 비우기, 장바구니 품목 삭제, 상품 삭제 |

`ConfirmModal` 공용 컴포넌트를 신설해 기존 상품 삭제 확인 모달과 동일한 스타일(`confirmOverlay`/`confirmCard`)을 재사용한다.

## 검증 결과

- `pnpm check` 통과
- Vitest **14개 파일·55개 테스트** 통과 (토스트 규칙 8건 포함)
- 모바일 미리보기: 잘못된 비밀번호로 로그인 시 상단에 오류 토스트가 표시되고, 접근성 속성이 `role="alert"`, 힌트 `오류 알림: 로그인. 아이디 또는 비밀번호가 올바르지 않습니다.`로 노출됨을 확인. 닫기 버튼(`알림 닫기`)도 함께 렌더링됨.

## 검증 중 발견·수정한 로그인 결함

토스트 검증 과정에서 `mif` / `@mif` 정상 자격으로도 로그인이 실패하는 문제를 발견했다. 원인은 P0 해시 전환 시 거래처 테스트 계정의 `passwordHash`가 요청 규격인 `@mif` 대신 `mif` 기준 해시로 저장된 것이었다.

| 항목 | 수정 전 | 수정 후 |
| --- | --- | --- |
| `mif` 계정 해시 | `sha256("mif-local-auth-v1:mif")` | `sha256("mif-local-auth-v1:@mif")` |
| 단위 테스트 | `mif`/`mif` 조합을 성공으로 기대 | `mif`/`@mif` 성공, `mif`/`mif` 거부를 함께 검증 |

수정 후 미리보기에서 `mif` / `@mif` 로그인 시 완료 토스트 `로그인 완료 / MIF 거래처 테스트 거래처 계정으로 로그인했습니다.`가 표시되고 홈 화면으로 진입하는 것을 확인했다. 관리자 계정 `admin` / `admin1234`는 기존 해시가 정상이어서 영향이 없다.
