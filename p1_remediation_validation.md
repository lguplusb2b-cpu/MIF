# MIF P1 보완 및 서버 연동 검증 기록

작성일: 2026-08-19 (Asia/Seoul)

## 검증 환경

| 구분 | 값 |
| --- | --- |
| DB | 로컬 PostgreSQL 16, DB `mif`, 사용자 `mif` |
| 스키마 | `db/schema.sql` 적용 (`mif_sessions` 테이블 포함) |
| API 서버 | `PORT=4000`, `DATABASE_URL=postgres://mif:mifpass@127.0.0.1:5432/mif` |
| 앱 미리보기 | `EXPO_PUBLIC_MIF_API_URL=http://127.0.0.1:4000`, 포트 8087 |
| 계정 | `admin/admin1234`(관리자), `mif/@mif`(거래처 A), `mif2/mif2pass`(거래처 B, 격리 검증용) |

## 다중 기기 검증 (`scripts/verify-multi-device.mjs`) — 17/17 통과

| 시나리오 | 결과 |
| --- | --- |
| 같은 관리자 계정의 두 기기 세션이 각각 발급된다 | PASS |
| 거래처 기기 세션이 서버에서 확인된다 | PASS |
| 잘못된 토큰의 스냅샷 요청은 401로 거부된다 | PASS |
| 관리자 기기에서 카테고리·상품을 등록한다 | PASS |
| 거래처 기기 스냅샷에 관리자 등록 상품이 즉시 나타난다 | PASS |
| 거래처 토큰의 상품 등록은 403으로 차단된다 | PASS |
| 거래처가 배송지를 등록하고 주문을 생성한다 (`MIF-20260819-0001`) | PASS |
| 다른 관리자 기기에서 새 주문을 즉시 확인한다 | PASS |
| 관리자 기기의 상태 변경이 거래처 기기에 `PAID`로 동기화된다 | PASS |
| 거래처 토큰의 주문 상태 변경은 403으로 차단된다 | PASS |
| 로그아웃한 기기의 토큰은 즉시 무효화된다 | PASS |
| 한 기기 로그아웃이 다른 기기 세션을 끊지 않는다 | PASS |
| 삭제한 상품은 거래처 스냅샷에서 즉시 제외된다 | PASS |
| 검증 주문·배송지 삭제 후 빈 운영 상태로 복구된다 | PASS |

## 거래처 격리 검증 (`scripts/verify-tenant-isolation.mjs`) — 6/6 통과

| 시나리오 | 결과 |
| --- | --- |
| 거래처 A가 주문을 생성한다 | PASS |
| 거래처 B 스냅샷에 거래처 A 주문이 노출되지 않는다 | PASS |
| 거래처 B 스냅샷에 거래처 A 배송지가 노출되지 않는다 | PASS |
| 거래처 B는 거래처 A 주문을 삭제할 수 없다 (403) | PASS |
| 거래처 B는 거래처 A 배송지를 삭제할 수 없다 (403) | PASS |
| 상품 카탈로그는 두 거래처 모두에게 공유된다 | PASS |

### 검증 과정에서 수정한 서버 결함

1. `POST/PATCH /api/addresses`가 클라이언트의 `companyId`를 필수로 요구해 세션만 가진 앱에서 400 오류가 발생했다.
   세션의 소속 거래처를 우선 사용하도록 `resolveCompanyId`를 도입했다.
2. `DELETE /api/orders/:id`와 `DELETE /api/addresses/:id`에 소유권 검증이 없어, 다른 거래처의 주문·배송지를
   삭제할 수 있었다. 관리자만 전체 삭제가 가능하고 거래처는 자기 소속만 삭제하도록 403 차단을 추가했다.
3. 주문 생성은 활성 결제 계좌가 없으면 409로 차단된다(P0 규칙). 검증용 계좌를 시드해 통과를 확인했다.

## 앱 미리보기 확인

- 서버 연동 모드(`EXPO_PUBLIC_MIF_API_URL` 설정)에서 앱이 정상 로드되고 첫 화면에 로그인 시트가 강제 표시된다.
- 로그인 화면에 회원가입하기·비밀번호 찾기 링크가 유지된다.
- 앱에서 `mif/@mif` 로그인을 실행하면 서버 `mif_sessions` 테이블에 세션 행이 실제로 생성된다(웹 미리보기 확인).
  즉 앱은 로컬 인증이 아닌 서버 인증 결과로 세션을 구성한다.

### 배포 시 필수 환경 변수

| 변수 | 대상 | 설명 |
| --- | --- | --- |
| `EXPO_PUBLIC_MIF_API_URL` | 앱 | 미설정 시 앱은 로컬 저장 모드로 동작하며 기기 간 공유가 되지 않는다. |
| `DATABASE_URL` | 서버 | PostgreSQL 연결 문자열. |
| `MIF_ALLOWED_ORIGIN` | 서버 | 기본값 `mif://`. 네이티브 앱만 쓰면 그대로 두고, 웹 클라이언트를 붙이면 해당 오리진을 지정해야 CORS가 통과한다. |
| `MIF_STORAGE_*` | 서버 | 상품 이미지 S3 업로드용. 미설정 시 이미지 업로드는 비활성화된다. |

## 정적 검사

- `pnpm check` 통과 (TypeScript 오류 없음)
- `pnpm test` 통과: 13개 테스트 파일 · 47개 테스트
