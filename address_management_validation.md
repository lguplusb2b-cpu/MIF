# 배송지 주소 모달 및 관리 검증 기록

## 구현 범위

- 배송지 관리 화면의 상단 `+` 버튼으로 배송지를 직접 추가할 수 있도록 연결했다.
- 주소 필드는 전용 **주소 선택** 모달을 열며, 우편번호·도로명 주소·지번 주소를 입력한 뒤 `이 주소 적용`으로 배송지 폼에 반영한다.
- 모든 시트와 주소 선택 모달에 `KeyboardAvoidingView`와 키보드 드래그 해제 처리를 적용해 Android·iOS에서 입력 영역이 키보드에 가려지지 않도록 했다.
- 보조 배송지를 기본 배송지로 변경할 수 있고, 기본 배송지가 삭제되면 남은 첫 배송지를 기본 배송지로 승격한다.

## 미리보기 검증

1. 테스트 데이터의 `테스트 본사` 배송지가 있는 상태에서 배송지 관리 화면을 열고 상단 `+` 버튼을 확인했다.
2. `강남 사무소` 배송지 등록 시 주소 선택 모달을 열어 `06236`, `서울특별시 강남구 테헤란로 152`, `역삼동 736`을 입력하고 적용했다.
3. 수령인·연락처·상세 주소를 입력한 뒤 기본 설정을 해제해 보조 배송지로 저장했다.
4. 목록의 `기본 배송지로 설정`으로 강남 사무소를 기본 배송지로 전환했고, 기존 본사 배송지의 기본 표시는 즉시 해제됐다.
5. 강남 사무소를 `강남 물류사무소`로 수정해 주소 모달 값과 상세 주소가 유지된 채 저장되는 것을 확인했다.
6. 수정된 보조 배송지를 삭제했고, 목록에는 기본 배송지인 테스트 본사만 남는 것을 확인했다.

테스트 종료 후 **빈 상태로 초기화**를 실행해 테스트 전용 배송지·상품·주문·계정·알림 데이터를 제거했다. MIF는 실제 운영 데이터가 없는 초기 빈 상태로 복귀했다.
