# 전체상품 고정·카테고리 중복 제거 검증 기록

## 적용 규칙

상품 카테고리 바의 **전체상품**은 데이터 카테고리와 분리된 첫 번째 고정 항목으로 유지한다. 실제 카테고리는 그 뒤에서 노출 순서대로 표시하며, `ALL` 식별자 또는 `전체상품` 이름의 합성 항목은 제외한다. 같은 ID 또는 공백·대소문자를 정규화한 같은 이름은 가장 앞선 카테고리 하나만 남긴다.

관리자 상품 등록·수정 시트와 관리자 카테고리 관리 화면도 동일한 중복 제거 목록을 사용한다. 서버 스냅샷 수신 단계에서도 같은 정규화를 적용해 다른 기기에서 동기화된 중복 카테고리가 다시 표시되지 않도록 했다.

## 미리보기 진행 상태

최신 Expo 웹 미리보기를 새 주소로 로드했고, 관리자 홈 화면이 정상 렌더링되는 것을 확인했다. 이어 상품 화면과 관리자 카테고리 관리 화면의 카테고리 순서를 확인한다.

상품 목록의 카테고리 바는 **전체상품 → 양념육 → 포장육 → 닭발** 순으로 표시됐으며, 이전에 중복됐던 양념육은 한 번만 노출됐다. 관리자 상품 등록 시트의 카테고리 선택기도 **양념육 → 포장육 → 닭발 → 직접 입력** 순으로 한 번씩만 표시되는 것을 확인했다.

상품 등록 시트를 닫고 관리자 홈 화면으로 돌아가, 관리자 카테고리 관리 화면의 중복 제거도 이어서 확인할 수 있는 상태를 만들었다.

관리자 카테고리 관리 화면에서도 양념육은 노출 순서 1에 한 번만 표시됐고, 이어 포장육·닭발 및 비활성 카테고리가 각각 한 번씩 표시됐다. 중복 양념육 행은 다시 나타나지 않았다.
