# 관리자 카테고리 정렬 검증

## 구현 범위

- 카테고리 행의 `⋮⋮` 핸들을 길게 눌러 드래그 앤 드롭으로 순서를 바꿀 수 있다.
- 각 행의 위·아래 화살표는 드래그가 어려운 환경에서도 동일한 순서 변경을 제공한다.
- 변경한 순서는 `sortOrder`로 다시 저장되며, 활성 카테고리의 상품 필터 및 상품 등록 선택지에도 즉시 적용된다.
- 첫 번째 항목의 위 화살표와 마지막 항목의 아래 화살표는 비활성화한다.

## 검증 시작 상태

드래그 목록 제스처 런타임을 추가한 뒤 최신 Expo 웹 미리보기가 오류 없이 로드됐다. 이후 테스트 데이터를 불러오고 관리자 권한에서 화살표 이동과 드래그 핸들 표시를 확인한다.

빈 상태 미리보기에서 테스트 데이터를 불러오자 신선식품·가공식품·소모품 카테고리와 각각 연결된 상품이 복원됐다. 이제 관리자 역할에서 순서를 바꾼 뒤 상품 필터와 등록 선택지가 같은 순서로 갱신되는지 확인한다.

테스트 데이터 로드 후 관리자 역할로 전환해 관리자 업무 메뉴가 정상 노출되는 것을 확인했다. 다음 단계에서 카테고리 관리의 드래그 핸들·위아래 버튼을 확인한다.

관리자 카테고리 관리 화면에 각 행의 `⋮⋮` 드래그 핸들, 위·아래 화살표, 수정·삭제 제어가 표시됐다. 첫 번째 항목에는 위 이동이, 마지막 항목에는 아래 이동이 표시되지 않아 경계 이동이 차단됐다. 가공식품의 위 이동 버튼을 눌러 가공식품→신선식품→소모품 순서로 변경됐고, 화면의 노출 순서도 각각 1·2·3으로 즉시 다시 계산됐다.

관리 화면을 닫고 상품 탭을 열자 카테고리 필터가 `가공식품 → 신선식품 → 소모품` 순으로 즉시 바뀌었다. 단위 테스트에서는 드래그 완료에 사용하는 재배치 규칙도 같은 순서를 저장하는 것을 확인했다.

검증 후 **빈 상태로 초기화**를 실행해 테스트 전용 카테고리·상품·계정·주문 데이터를 제거했다. MIF 미리보기는 실제 운영 데이터가 없는 초기 빈 상태로 복귀했다.
