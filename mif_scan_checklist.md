# MIF 앱 전체 프로세스 스캔 및 보완 체크리스트

작성: Manus AI · 기준일 2026년 8월 19일 · 대상 커밋 `5211168` (main)

## 1. 스캔 개요

MIF 앱의 전체 업무 프로세스를 코드 레벨과 실제 미리보기 동작 양쪽에서 점검했다. 점검 범위는 인증과 권한, 상품 등록·탐색, 장바구니와 배송 조건, 주문 생성과 상태 전이, 관리자 운영 메뉴, 알림, 그리고 서버 API 연결 상태다. 기술 품질 게이트인 타입 검사와 단위 테스트는 모두 통과했으며, 실제 결함은 주로 **화면 피드백**, **권한 분리**, **서버 연동 범위**에서 발견됐다.

| 점검 항목 | 결과 |
| --- | --- |
| 타입 검사 (`tsc --noEmit`) | 오류 0건 |
| 단위 테스트 (Vitest) | 12개 파일·42개 테스트 전부 통과 |
| 코드 규모 | App.tsx 6,752줄 단일 파일, 화면 22개·시트 13개 |
| 업무 규칙 모듈 | 9개 파일로 분리 (workflows, product, category, order-presentation 등) |
| 서버 라우트 | Express 47개 |
| 앱이 실제 호출하는 API | 8개 |
| 미리보기 프로세스 테스트 | 상품 등록 → 담기 → 배송지 → 주문 생성 → 상태 전이 전 구간 통과 |

핵심 프로세스는 끊김 없이 동작한다. 관리자로 상품을 등록하면 목록에 즉시 반영되고, 최소 수량이 자동 적용된 담기와 계속 담기 모달이 작동하며, 배송지 저장 후 택배를 선택해 주문하면 `MIF-20260819-0001` 주문이 생성되고 목록 상태 칩과 알림 배지까지 갱신된다. 즉 **업무 데이터 계층은 정상**이고, 남은 문제는 사용자에게 보이는 피드백과 운영 안전장치다.

## 2. 발견된 주요 문제

### 2.1 확인된 버그: 주문 상세 상태 미갱신

주문 상세에서 "결제확인 처리"를 눌러도 상세 화면의 상태 배지가 계속 `접수`로 남는다. `changeOrderStatus`가 `data.orders`만 갱신하고, 시트에 전달되는 `selectedOrder` 상태는 갱신하지 않기 때문이다. 목록으로 나가면 정상 반영되지만, 상세 화면에서 접수 → 결제확인 → 상품준비로 연속 처리할 수 없다. 관리자 업무 효율에 직접 영향을 주는 결함이다.

### 2.2 권한 분리 미흡

관리자 계정으로 로그인한 상태에서도 상품을 장바구니에 담고 주문을 생성할 수 있었고, 생성된 주문의 거래처가 "MIF 거래처"로 기록됐다. 관리자가 발주 주체로 기록되면 주문 데이터의 신뢰성이 훼손된다. 같은 맥락에서 관리자 더보기 화면에 "거래처 가입 신청"과 "비밀번호 재설정" 진입이 남아 있다.

### 2.3 화면 피드백 부재

검증 실패와 오류 안내가 전부 `Alert.alert`(35곳)로 처리되어 있다. 모바일 기기에서는 정상 표시되지만, 웹 미리보기에서는 아무 메시지도 나타나지 않아 **무음 실패**로 보인다. 실제로 잘못된 비밀번호 로그인, 필수 항목 누락 상품 저장, 배송 조건 누락 주문 시도 모두 화면 안내 없이 차단됐다.

### 2.4 운영 공백

결제 계좌가 등록되지 않은 상태에서도 주문이 생성되어, 장바구니와 주문 상세에 "등록된 결제 계좌가 없습니다."만 표시된다. 거래처가 입금할 계좌를 알 수 없는 주문이 만들어지는 구조다. 또한 상품 삭제·판매중지 수단이 없고, 알림은 삭제할 수 없어 계속 누적된다.

### 2.5 서버 연동 범위

서버에는 라우트가 47개 있지만 앱이 호출하는 API는 8개다. `EXPO_PUBLIC_MIF_API_URL`이 없으면 앱 전체가 로컬 AsyncStorage 모드로 동작하므로, **기기마다 데이터가 분리되어 관리자와 거래처가 같은 주문을 공유할 수 없다.** 실제 다중 사용자 운영에 들어가기 전 반드시 해소해야 하는 구조적 격차다.

## 3. 보완 체크리스트

### P0 — 운영 전 필수

- [ ] 주문 상세 시트 상태 즉시 반영: `changeOrderStatus`에서 `selectedOrder`도 갱신해 상세에서 연속 상태 처리 가능하게 수정
- [ ] 관리자 계정의 발주 차단: 관리자 역할일 때 담기·주문하기 버튼 비활성화 또는 장바구니 탭 숨김
- [ ] 관리자 화면에서 거래처 가입 신청·비밀번호 재설정 진입 제거
- [ ] 결제 계좌 미등록 시 주문 차단 또는 관리자 계좌 등록 유도 안내 표시
- [ ] 웹·앱 공통 인앱 토스트/인라인 오류 메시지 도입해 `Alert.alert` 의존 제거 (로그인 실패, 저장 검증 실패, 주문 조건 누락 우선)
- [ ] 상품 저장 검증 실패 시 어떤 항목이 누락됐는지 필드 단위로 표시
- [ ] 상품 삭제 또는 판매중지 기능 추가 (오등록 상품 회수 경로 확보)
- [ ] 로컬 계정 평문 비밀번호 제거: `src/domain.ts`의 `password: "mif"`, `password: "admin1234"`를 해시 또는 서버 인증으로 이전

### P1 — 실사용 품질

- [ ] 서버 API 연동 전환: 상품·주문·배송지·결제계좌·공지·Q&A를 서버 기준으로 이동해 다중 사용자 데이터 공유 확보
- [ ] 자동 로그인(세션 유지) 구현: 앱 재실행 시 매번 재로그인하는 문제 해소
- [ ] 로그인 후 비밀번호 변경 화면 추가 (현재는 재설정 "요청"만 존재)
- [ ] 송장 번호·택배사 입력 UI와 배송조회 진입 추가 (서버 필드는 이미 존재)
- [ ] 용달 기사 연락처 표시·전화 연결 추가
- [ ] 관리자 주문 CSV 내보내기 버튼 연결 (`GET /api/orders/export` 활용)
- [ ] 상품 이미지 S3 업로드 연결 (`POST /api/uploads/product-image` 활용)
- [ ] 알림 항목 탭 시 해당 주문·공지 상세로 이동하는 딥링크 연결
- [ ] 알림 삭제 또는 보관 기간 정책 추가
- [ ] 관리자도 거래처와 동일한 상품 상세를 볼 수 있는 경로 추가 (현재는 수정 시트만 열림)

### P2 — 완성도·확장

- [ ] 주소 검색 API 연동(도로명주소/우편번호 검색)으로 수동 입력 오류 방지
- [ ] 상품 목록 페이지네이션 또는 가상 스크롤 적용 (현재 전체 목록 일괄 렌더링)
- [ ] 상품 상세 이미지 확대 보기 추가
- [ ] 찜 목록 공유 링크 연동 (`/api/favorite-shares` 활용)
- [ ] 수량 기반 재고 관리와 주문 시 재고 차감 검토 (현재는 재고 있음·품절 2단계)
- [ ] App.tsx(6,752줄) 화면 단위 파일 분리로 유지보수성 확보
- [ ] ESLint 등 정적 분석 스크립트 추가 (현재 린트 게이트 없음)
- [ ] 거래처 계정 기준 상태 변경 알림 수신 검증 테스트 추가

## 4. 권고 순서

가장 먼저 P0의 주문 상세 상태 반영과 관리자 발주 차단을 처리하는 것이 좋다. 두 항목은 코드 수정 범위가 작고 운영 데이터 신뢰성에 직접 영향을 준다. 이어서 인앱 토스트를 도입하면 나머지 검증 로직을 손대지 않고도 사용자가 겪는 "무음 실패" 문제 대부분이 해소된다. 그 다음 단계로 서버 API 전환과 자동 로그인을 진행하면, 단일 기기 시연용 앱에서 실제 다중 사용자 운영 앱으로 성격이 바뀐다.

검증에 사용한 테스트 상품·주문·배송지는 모두 삭제해 MIF는 빈 데이터 상태를 유지하고 있다.
